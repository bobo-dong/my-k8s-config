apiVersion: v1
kind: Service
metadata:
  name: my-demo-app
  namespace: default
spec:
  selector:
    app: my-demo-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
